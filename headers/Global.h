#ifndef GLOBAL_H
#define GLOBAL_H

#include "./Book.h"
#include "./User.h"

// int MAX_BOOKS = 10;
// int MAX_USERS = 10;

bool compareByName(const Book& a, const Book& b);
bool compareById(const Book& a, const Book& b);

#endif